#include <stdio.h>
#include <string.h>
#include <stdlib.h>

char** bigrammiArray(char* parola, int* num);

void printBigrammi(char* bigrammi[], int numeroBigrammi);

void freeBiagrammaArray(char** bigrammi, int numeroBigrammi);
